# ローカル開発: Cognito を使わずローカルトークンで認証
VITE_AUTH_MODE=local

# Optional: override WS URL (defaults to same host)
# VITE_WS_URL=wss://your-cloudfront-domain.net

# Note: VITE_COGNITO_USER_POOL_ID / VITE_COGNITO_CLIENT_ID は不要になりました。
# 本番環境では CDK が /config.json を S3 に自動デプロイし、フロントエンドがランタイムで読み込みます。
